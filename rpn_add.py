#!/usr/bin/env python3

#
# This function is really too trivial to warrant its own module, but that
# is nevertheless the approach we will take with this and other operators.
#

def add(a, b):
   # The expression below contains an obvious error.  Fix the error and
   # remove this comment.
   #
   return a + b + 1
